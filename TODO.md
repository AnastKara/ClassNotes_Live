# TODO

- [ ] Fix blank UI by enabling Tailwind processing in Vite config (add `@tailwindcss/vite` plugin).
- [ ] Restart dev server + hard refresh to verify UI appears.
- [ ] If still blank: inspect browser console/network and check SSR/route rendering or CSS visibility.

